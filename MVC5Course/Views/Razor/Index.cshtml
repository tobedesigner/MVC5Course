@{
    ViewBag.Title = "<h1>Hello Good Morning</h1>";
}
@ViewBag.Title

@Html.Raw(ViewBag.Title)



<ul>
    @for (int i = 0; i < 10; i++)
    {
        <li>@i</li>
    }
</ul>

<hr />
@{
    var items = new int[] { 1, 2, 3, 4, 5 };
}

@for (int i = 0; i < items.Length; i++)
{
    @items[i]
}


@for (int i = 0; i < items.Length; i++)
{
    @(items[i] + " ")
}


<ul>
@for (int i = 0; i < items.Length; i++)
{
  <li>@items[i]</li>
}
</ul>








@foreach (var item in items)
{
    @item
}


@for (int i = 0; i < items.Length; i++)
{
    @*@items[i]@(" ")*@
    @(items[i] + " ")
}


<ul>
@foreach (var item in items)
{
  <li>@item</li>
}
</ul>
